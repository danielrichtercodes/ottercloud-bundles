# ottercloud-bundles
> TODO: create proper readme
> ottercloud-bundles is a opinionated collection of kubernetes artifacts (helm and kustomize)
> that are shared between clusters i run and are meant to be installed in a cluster using flux.
> Examples of bundles are: preconfigured metallb pools, kyverno baseline policies, certmanager cluster issuer config, etc.

## TODO
- instead of installing the app and it configuration separately extend the chart and add configuration